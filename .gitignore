# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
# ...except the release build we hand out, which is committed on purpose.
!dist/*.apk
*.keystore
!debug.keystore
.kotlin/

# Local SDK/JDK paths must never be committed; each machine writes its own.
# Signing credentials belong here too, never in the tree.
keystore.properties
